{
  "name": "neet-buddy-static",
  "version": "1.0.0",
  "private": true,
  "description": "Deployable static build of NEET Buddy. These files are already built - no build step is required on the host.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "echo \"Already built - static files only\"",
    "start": "echo \"Static site - served directly by Apache/LiteSpeed\""
  }
}
